{
  "name": "freepilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "react-native": "0.83.6"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "mobile:dev": "npm run start --workspace @freepilot/mobile",
    "mobile:android": "npm run android --workspace @freepilot/mobile",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "android:add": "npm run build && cap add android",
    "android:sync": "npm run build && cap sync android",
    "android:open": "cap open android",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@capacitor/android": "^7.6.5",
    "@capacitor/cli": "^7.6.5",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "typescript": "^5.6.3",
    "vite": "^8.0.13",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.5",
    "@freepilot/finance-core": "*",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}
